# Plan: Correcting Sections 2.6 and 2.7 of the Diploma Document

The user has identified that sections 2.6 and 2.7 of the diploma document contain "outright lies" about the project. Based on the codebase analysis, these sections describe features (analytics/charts and specific UI elements like BottomNavigation) that are not actually implemented in the current Python/Kivy project.

## Phase 1: Analysis of Discrepancies

### Section 2.6: Analytics and Statistics
- **Document claims:** Implementation of Bar Charts and Line Charts for productivity tracking.
- **Reality:** `DatabaseManager.get_stats()` and `TaskService.get_statistics()` only return raw counts (total, active, completed, overdue). There is no code for rendering charts or tracking daily/weekly progress in `ui/`.

### Section 2.7: User Interface
- **Document claims:** Use of `MDBottomNavigation` for navigation and a "Profile Activity Block" with percentages.
- **Reality:** `main.py` uses a simple `BoxLayout` with two `MaterialButton` widgets ("Задачи" and "Архив") for navigation. There is no `MDBottomNavigation` or complex profile widget.

## Phase 2: Proposed Corrections

### Section 2.6 (Analytics)
- **Change:** Remove mentions of Bar/Line charts.
- **New focus:** Describe the statistical aggregation logic implemented in `DatabaseManager` and `TaskService`. Mention that the system provides a quantitative overview of task statuses (Active, Completed, Overdue) which is used by the AI Assistant to report progress.

### Section 2.7 (UI Design)
- **Change:** Replace `MDBottomNavigation` with the actual tab-based navigation implemented in `main.py`.
- **Change:** Remove the "Profile Activity Block" and describe the actual layout: a top tab bar for switching between "Tasks" and "Archive", and the `TaskListScreen` which focuses on the task list and AI chat access.
- **Update Table 2.9:** Ensure the UX elements listed match the actual implementation (e.g., `MaterialButton`, `RecycleView`, `ChatModal`).

## Phase 3: Implementation Steps

1.  **Read the diploma document** again to identify the exact text to be replaced in sections 2.6 and 2.7.
2.  **Draft the "truthful" version** of these sections based on the actual code in `main.py`, `services.py`, and `ui/`.
3.  **Apply edits** to the diploma document.

## Phase 4: Verification
- Verify that the updated text accurately reflects the classes and methods found in the codebase (e.g., `TaskService.get_statistics`, `MaterialRoot`, `ScreenManager`).
